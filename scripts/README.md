# Scripts

Place local workflow helpers here.

Expected future scripts:

- `play_match.py`: run two agents against each other.
- `self_play.py`: collect self-play games.
- `evaluate.py`: run benchmark matches.
- `train.py`: launch a training job from a config file.
